#For full documentation of the parameters in this file, and a list of all the
#parameters available for GlobalPlanner, please see
#http://wiki.ros.org/carrot_planner
base_global_planner: "carrot_planner/CarrotPlanner"
CarrotPlanner:
    #step_size: 0.0           # The size steps to take backward in meters along the vector between the robot and the user-specified 
                              ## goal point when attempting to find a valid goal for the local planner.
                              ## Default is resolution of the associated costmap.

    min_dist_from_robot: 0.1  # The minimum distance from the robot in meters at which a goal point will be sent to the local planner.